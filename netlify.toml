[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback (React Router) — 纯静态，所有路由回退到 index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
